# 同位素杂质含量计算方法与原理说明

**版本**：v1.2　**日期**：2026-09-23
**适用样品**：SPH20291（天然型对照）与 SPH20291-Isotope1（全标记稳定同位素内标）
**用途**：本文档描述同位素修饰多肽对照品中「未完全标记杂质」的含量计算方法与全部原理，
供第三方审核计算过程的合理性、可复现性与结论可信度。

**修订记录**
- **v1.2（2026-09-23）**：修正 §7.3 / §7.7 中「M0 法与基峰法数学等价」的过强表述——
  两法位置重合、量级一致，但链端存在固有偏差，二者并非严格等价；补充合成谱闭环验证证据。
- v1.1（2026-08-23）：初版。

---

## 1. 背景与目标

SPH20291-Isotope1 是一个**全标记稳定同位素内标**：分子中部分 ¹²C 被 ¹³C、部分 ¹⁴N 被 ¹⁵N 替换。
在合成、纯化过程中，会存在「标记不完全」的分子——即 1 个或若干标记原子仍是天然同位素（¹²C / ¹⁴N）。
这些分子质量比本体轻、在质谱中和本体同位素包络重叠，其含量需用质谱定量。

本文档回答两个问题：

1. **各杂质含量是多少**（以 9 档体系报告，见 §5）；
2. **该含量是否可信**（通过四种独立算法互洽、分辨率稳健性扫描、天然型阴性对照三方面验证）。

计算方法分两条独立路线，互为交叉验证：

- **路线 A：理论系数 + 逐级三角扣除**（主定量，`同位素杂质计算/run_all.py`，输出
  `sp_003_杂质含量.xlsx`、`STD_0.005_验证.xlsx`）；
- **路线 B：NNLS 解卷积**（独立拟合，`解卷积/deconv_impurity.py`，输出
  `deconv_impurity_output.xlsx`）。

两条路线共享同一套理论同位素分布引擎（`theo.py`）与杂质枚举器（`imp.py`）。

---

## 2. 分析物定义

### 2.1 SPH20291（天然型）

| 项目 | 值 |
|---|---|
| 分子式 | C₁₃₄H₁₉₈N₂₈O₃₅S₂ |
| 单同位素质量（Exact Mass） | 2823.4015 Da（ChemDraw 标称 2823.40） |
| M0 m/z（z=3） | 942.1411 |
| M0 m/z（z=4） | 706.8577 |
| 同位素标记原子 | 0（全天然） |

### 2.2 SPH20291-Isotope1（全标记本体）

| 项目 | 值 |
|---|---|
| 分子式 | C₁₂₈[¹³C]₆ H₁₉₈ N₂₆[¹⁵N]₂ O₃₅S₂ |
| 单同位素质量（Exact Mass） | 2831.4157 Da（ChemDraw 标称 2831.42） |
| M0 m/z（z=3） | 944.8125 |
| M0 m/z（z=4） | 708.8612 |
| 标记原子总数 | 8 = 6×¹³C + 2×¹⁵N |

> **符号约定**：本文 `[¹³C]` 表示指定同位素 ¹³C；未标注的 C/N 为天然同位素分布
> （¹²C/¹³C、¹⁴N/¹⁵N 按 NIST 天然丰度）。

---

## 3. 元素与质量数据源

- 同位素质量与天然丰度来自 **molmass** 库（底层为 NIST 数据）。
- ESI 加合离子模型：正离子按 `[M + z·H]ᶻ⁺` 处理，
  `m/z = (M + z·(m_H − m_e)) / z`，其中
  `m_H = 1.00782503223 u`（¹H 原子质量），`m_e = 0.000548579909 u`（电子质量）。
- 单同位素峰（M0）= 全部元素取最轻同位素（天然元素）或指定同位素质量（标记元素）时
  的质量。本文所有「理论 M0 m/z」均按此计算。

---

## 4. 理论同位素分布计算（`theo.py`）

### 4.1 算法：多项式卷积展开

对元素表 `(E, isotope, count)` 的每一列：

1. **单原子分布**：
   - `natural`：取该元素全部天然同位素 `[(m_i, p_i)]`，概率归一化到总和 1；
   - 指定同位素（如 `13`、`15`）：取单一质量点 `[(m, 1.0)]`。
2. **一列 count 个原子**：单原子分布自卷积 count 次
   （DP 按「质量偏移 ×10⁶ + 概率」累积，避免大数漂移）。
3. **逐列卷积**：把各元素列的分布依次卷积合并；每列卷积后按质量容差
   `tol = 0.001 Da` 聚类合并近简并峰，并剪除概率 `< 1e-16` 的峰。
4. 输出丰度最高的前 `top_n` 个峰（默认 50），每个峰带
   中性精确质量、m/z（按 §3 电荷模型）、相对丰度（最强峰 = 100%）。

### 4.2 与实验的一致性

- 对天然型 SPH20291（z=3），理论 M0 = 942.1411，实测峰心 = 942.1430，
  质量偏移 **+1.98 ppm**（< 5 ppm 容差）——谱图读取与理论计算自洽（见 §11）。
- 该引擎同样被杂质枚举（§5）、逐级扣除系数矩阵（§7）、解卷积基模式（§9）共用，
  保证三条路径的理论一致。

---

## 5. 杂质枚举与 9 档体系（`imp.py`）

### 5.1 杂质枚举

标记分子（8 个标记原子）与天然分子之间的全部同位素取代杂质：

- 每个标记列 `(E, iso, n)` 中，把其中 `k` 个标记原子替换回天然同位素，`k = 1..n`；
- 多个标记列做笛卡尔积，排除全部 `k=0`（即本体本身）；
- 同分子式不同排列顺序合并去重。

对 SPH20291-Isotope1：C 标记 6 个 + N 标记 2 个 → `(6+1)(2+1) − 1 = 20` 个杂质。

### 5.2 9 档体系

按「被替换回天然的标记原子数」归并：

```
tier t = t 个标记原子被天然同位素替换（t = 0..8）
```

- **tier 0**：本体 SPH20291-Isotope1（全标记，8 个标记原子全部保留）
- **tier 8**：天然形 SPH20291（8 个标记原子全部被替换回天然）
- 每档含若干不可分辨组合（如 tier-1 可能是 1 个 ¹³C 换回、或 1 个 ¹⁵N 换回）。

> **质量简并说明**：¹³C/¹⁵N 替换组合间的质量差极小（@z=3 约 0.0021 Da），
> 远低于仪器分辨极限（约 0.015 Da @30k 分辨率），实验上落在同一峰，
> 无法分辨。故「杂质明细」中**每档合并为 1 行**（列出全部分子式与各自真实
> 单同位素 m/z），含量取**整档总量**，不再按成员均分。

---

## 6. 谱图读取与预处理

### 6.1 数据源

- **路线 A**：岛津 LCMS-9030（Q-TOF，标称分辨率 30k）导出的 **mzML**（profile/质心谱）。
  `mzml_io.py` 流式解析，自动在目标 m/z 附近检测 RT 出峰窗口
  （超过最强信号 10% 的连续 RT 段），窗口内全部扫描按 **m/z 分箱（0.001 Da）累加**
  得到一条轮廓谱——质心化谱峰位不固定，不能按索引相加，故分箱累加。
- **路线 B**：`.lcd` 原始数据（经 `lcd2csv.py` 导出为谱图 CSV）或已有谱图 CSV。

### 6.2 强度提取方式

在指定 m/z 窗口内提供两种强度：

- **积分**：窗口内强度求和（含整峰）；
- **峰顶**：窗口内最大单点强度。

### 6.3 本底扣除

自积分流程从峰附近平缓基线处扣本底：基线取测量窗外侧环（相邻档间距之内）的
**中位强度**，积分减「基线×箱数」，峰顶减「基线」。本样品为质心化谱，窗外无连续
本底，实测本底 ≈ 0，故扣与不扣结果一致。若数据源为 LabSolutions 已积分的 .lcd
则无需此步。

---

## 7. 路线 A：四法逐级三角扣除（主定量）

### 7.1 锚点与窗口

- 每档两个锚点：**M0**（单同位素峰）与**基峰**（理论包络中丰度最大峰）。
- 理论系数窗：5 ppm（用于重叠矩阵计算）；
- 实测提取/重叠窗：**±0.04 Da**（容纳整峰并吸收局部质量偏移）；
- 校准窗：±0.06 Da 内找真实峰心。

### 7.2 重叠矩阵（固定系数）

`W[i][j]` = tier j 的全部理论峰落在 tier i 锚点 ±0.04 Da 窗口内的概率之和。
系数全部来自 `theo.py` 理论包络，**不含实测谱信息**，即「固定系数」。

### 7.3 二对角链结构

9 档沿质量轴构成一条链，相邻档 M0 间距 ≈ 0.33/z Da（≈353 ppm @z=3），
远大于提取窗（0.08 Da），非相邻档不会串入。因此任一 M0 位置只有两档贡献：

```
位置 M0(t) 上的强度 = A(t)·P0(t) + A(t+1)·Pbase(t+1)
```

其中 `A(t)` 为 tier t 的量，`P0(t)` 为 tier t 自身 M0 在窗内概率，
`Pbase(t+1)` 为更轻一档 t+1 的基峰投影到本位置的串入。

> **关键位置重合**：5 ppm 下基峰(t) 与 M0(t−1) 位置重合，即每一档的基峰位置恰是
> 上一档的 M0 位置。这使「M0 法」与「基峰法」在量级上一致、可互相校验；
> 但两法**并非严格等价**——链端档位（t=0 的基峰位、t=8 的 M0 位）没有对偶档可供抵消，
> 扣除不闭合，存在固有偏差（实测见 §7.7）。

### 7.4 逐级扣除

- **M0 法**：轻 → 重（t=8 → 0），每档先扣掉相邻更轻档基峰的串入，再除以自身 M0 系数；
- **基峰法**：重 → 轻（t=0 → 8）对偶扣除。

```
A(t) = [ I(t) − A(t+1)·W(t, t+1) ] / W(t, t)     （M0 法，t 从 8 到 0）
```

扣除结果为负时截断为 0 并记 note（信号低于检出或校准偏差）。

### 7.5 四种估计量与归一化

| 估计量 | 锚点 | 强度方式 |
|---|---|---|
| M0·积分 | M0 | 窗口积分 |
| M0·峰顶 | M0 | 峰顶高度 |
| 基峰·积分 | 基峰 | 窗口积分 |
| 基峰·峰顶 | 基峰 | 峰顶高度 |

含量归一化到**全部物种总量 = 100%**：`含量% = 物种量 / Σ(本体 + 全部杂质) × 100`。

### 7.6 路线 A 结果（sp_003，M0·积分）

| 档 | 档名 | z=3 含量% | z=4 含量% |
|---|---|---|---|
| 0 | 本体（全标记） | **98.3** | 99.7 |
| 1 | 替换 1 档 | **1.7** | 0.3 |
| 2–8 | 替换 2–8 档 | 0.0 | 0.0 |

**判定**：sp_003 中 SPH20291-Isotope1 的同位素杂质以 **tier-1**（1 个标记原子被天然替换）
为主，含量约 **1.7%**（M0·积分）；更高替换档低于检出。z=4 时杂质处于噪声级
（≈0.3%），无法独立定量，仅作一致性校验。

### 7.7 四法互洽（z=3）

| 估计量 | 本体% | tier1% |
|---|---:|---:|
| M0·积分 | 98.331 | 1.669 |
| M0·峰顶 | 96.337 | 3.663 |
| 基峰·积分 | 96.227 | 3.773 |
| 基峰·峰顶 | 95.582 | 4.418 |

- 四种算法互洽（本体 95.6–98.3%），差异来自「窗口积分」vs「峰顶高度」的取强度方式：
  积分法偏低（含相邻峰拖尾），峰顶法偏高（噪声抬升），差异 1–2.7 个百分点。
  其中基峰法的偏差除取强度方式外，还叠加了链端固有偏差（见下条）。
- 基峰法与 M0 法**量级一致**（本体 96.2% vs 98.3%），验证了「5 ppm 下基峰(t) 与 M0(t−1)
  位置重合」这一前提；但两法**并非严格等价**：链端档位缺少对偶扣除项，存在固有偏差。
  合成谱闭环验证（设定本体 98.3% / tier1 1.7%，把理论包络合成谱喂回引擎）显示
  **M0·积分可精确还原**（98.300 / 1.700），而基峰法给出 tier1 ≈ 0.73%、tier2 ≈ 0.62%
  （设定 0），偏差 1–2 个百分点。
- **报告值采用 M0·积分**（最稳健：覆盖整峰、对质量漂移不敏感，且闭环可精确还原）。

### 7.8 分辨率稳健性扫描

对 sp_003 z=3、M0·积分法，扫描提取窗半宽 0.02 / 0.04 / 0.06 Da：

| 提取窗半宽 (Da) | 本体% | tier1% |
|---|---:|---:|
| 0.020 | 98.331 | 1.669 |
| 0.040 | 98.331 | 1.669 |
| 0.060 | 98.331 | 1.669 |

结果完全稳定 → 对分辨率/窗口选择不敏感，5 ppm 方案稳健。

---

## 8. 真实标记杂质表（扣除本体包络投影后的独立验证）

### 8.1 目的

上述 9 档结果隐含一个模型假设：本体档之外的全部强度都是杂质。但本体（全标记）
自身的天然同位素包络（128 个 ¹²C、26 个 ¹⁴N 的天然分布）会在质量轴高侧产生
M+1、M+2… 等伴峰。「真实标记杂质」表回答一个更严格的问题：
**扣掉本体自身同位素背景后，真实的标记杂质含量是多少？**

### 8.2 算法

1. 在 9 个锚点 k = 0..8（k = 被替换回天然的标记原子数）各取 ±0.04 Da 窗积分 `I[k]`；
2. 用**本体**的理论同位素包络（最小二乘振幅 A）预测本体在各锚点的投影 `P[k]`：
   `A = Σ(I[k]·p[k]) / Σ(p[k]²)`，`p[k]` 为本体包络落在锚点 k 窗口的概率；
3. **残余 = I[k] − P[k] = 真实标记信号**；
4. 含量 = 残余 / 总量 × 100%。

**物理关键**：全标记本体的包络峰全部位于 m/z ≥ 本体 M0（高质量侧），
**不会投影到低质量侧的杂质锚点**——因此低质量侧锚点（k=0..7）的残余
直接等于真实杂质，无大数相减损失精度。天然型对照则相反：天然包络的 M+k
峰恰好落在「含 k 个标记原子」杂质的 M0 处，用于检验算法是否会把天然样品
误判为含标记杂质。

### 8.3 结果

| 样品 | 本体 | 真实标记杂质合计 | 判定 |
|---|---|---|---|
| sp_003（z=3） | 98.4% | 1.6% | 与 M0·积分（98.3/1.7%）一致 |
| sp_003（z=4） | 99.7% | 0.3% | 噪声级，趋势一致 |
| STD 0.005（z=3） | 100.1% | ≈0% | 天然型阴性对照通过 |
| STD 0.005（z=4） | 102.2% | ≈-2.2%（噪声级） | 通过 |

> 该表独立于 §7 的扣除法，结论一致 → 报告值即扣除本体自身同位素背景后的
> 真实含量，无本体包络串扰。

### 8.4 表中「不带电荷单同位素质量」

每行给出该分子式对应的**单同位素精确质量**（换算公式
`M = m/z × z − z × 1.007276466812`），并给出实测 M0 换算后的误差 ppm。
该值与 ChemDraw 的 Exact Mass 一致（天然 2823.40、全标记 2831.42），
用于人工核对该分子式对应的质量是否正确。

---

## 9. 路线 B：NNLS 解卷积（`deconv.py` / `deconv_impurity.py`）

### 9.1 原理

不依赖逐级扣除假设，直接把实测谱表示为全部物种理论模式与仪器峰形的加权和：

```
实测谱 ≈ Σᵢ wᵢ × (模式ᵢ ⊗ 仪器峰形) + 基线
```

1. **基模式**：每个物种（本体 + 20 个杂质）用 `theo.py` 全模式
   （min_ab=0，前 top_n 峰），棒状谱；
2. **峰形卷积**：Gaussian 峰形，`FWHM = m / R`，R = 30000（LCMS-9030）；
3. **NNLS 拟合**：`scipy.optimize.nnls`，非负最小二乘
   `min ‖A·w − y‖², w ≥ 0`，设计矩阵每列归一化使权重直接 = 相对含量；
4. **质量偏移扫描**：对 -10..+10 ppm（步长 1 ppm）逐个拟合，取 RSS 最小者，
   吸收质量校准误差；
5. **质量简并类合并**：单同位素 m/z 间隔 < 1×FWHM 的物种合并为质量简并类
   （低于分辨极限的 ¹³C/¹⁵N 近简并对无法可靠分开），含量取类总量。

### 9.2 与路线 A 的关系

- 路线 A 用理论系数做固定系数的解析扣除；路线 B 用实测谱直接拟合。
- 二者共享理论引擎，是**独立的数据处理路径**，结果一致则互相背书。
- 质心谱点稀疏时，先做 centroid→synthetic profile 转换
  （每个质心峰替换为 Gaussian，bin = 0.01 Da）保证 NNLS 采样点充足。

### 9.3 路线 B 结果（z=3）

| 样品 | 本体 | 杂质档 |
|---|---|---|
| sp_003（全标记） | 100.0% | 无 |
| STD 0.005（天然） | 98.7% | 档2=0.5%、档3=0.8% |

> 注：路线 B 对 STD 的少量非零杂质档属于拟合残差在近简并类上的数值波动
> （STD 为纯天然样品，真实标记杂质≈0，见 §8.3），量级在噪声级。

---

## 10. 质量校准

逐锚点质量校准：在每个档理论 M0/基峰位置 ±0.06 Da 内找实测最强信号点作为
「真实峰心」，报告「真实峰心 − 理论」为校准残差。sp_003 z=3 各锚点校准残差
均 < 2 ppm（在 5 ppm 容差内）；z=4 因信号弱，tier0 锚点残差约 +6.7 ppm
（邻近噪声峰所致，但 ±0.04 Da 窗仍覆盖真实峰，本体回收 >99.7%）。

---

## 11. 验证实验（STD 0.005 天然型阴性对照）

STD 0.005 应只含天然型 SPH20291（无标记化合物）。用它检验整套流程：

1. **套标记引擎**：tier0（标记本体）= **0.000%**，≈0 → 确认样品无标记化合物；
2. **天然包络对照**：理论 M0 = 942.1411，实测峰心 = 942.1430，
   质量偏移 = +1.98 ppm → 谱图读取与理论正确；
3. **真实标记杂质表**：本体(天然) = 100.138%，标记杂质合计 ≈ 0 → 样品确为纯天然型。

结论：校准/提取流程不会凭空制造标记信号，定量基准可信。

---

## 12. 结论

1. sp_003 中 SPH20291-Isotope1 的同位素杂质以 **tier-1**（1 个标记原子被天然替换）
   为主，含量约 **1.7%**（M0·积分，报告值），更高替换档可忽略；
2. 四种算法互洽（本体 95.6–98.3%），推荐以 **M0·积分** 作报告值，其余作校验；
3. 「真实标记杂质」表独立验证：扣除本体包络后杂质仍 ≈1.6% → 报告值真实；
4. z=4 因杂质在噪声级仅作交叉验证；z=2 在 sp_003 中无杂质信号，已排除；
5. 结果对分辨率设定稳健（窗口扫描完全稳定）；
6. STD 阴性对照通过（tier0≈0，真实标记杂质≈0），校准与谱图读取正确；
7. 解卷积路线（独立拟合）趋势一致，进一步背书主结果。

---

## 13. 关键假设与局限

1. **理论系数可信**：全部系数来自 NIST/molmass 同位素数据。对 C>50 的大分子，
   同位素包络高度重叠，绝对丰度依赖理论系数，结论以**相对趋势**为准。
2. **质量简并不可分辨**：¹³C/¹⁵N 替换组合低于仪器分辨极限，报告中按档合并、
   含量取整档总量，不报告组合级细分。
3. **归一化语义**：含量为「相对摩尔量占比」（组分语义），区别于「最强峰=100%」
   的相对丰度。
4. **噪声级信号不定量**：含量 < ~1% 且与噪声同量级的档，报告为「≈0 / 噪声级」，
   不作定量结论。
5. **z=4 辅助性**：杂质信号弱，仅作趋势一致性校验。

---

## 14. 复现清单

### 14.1 输入数据

- sp_003：SPH20291-Isotope1（全标记）LC-MS 数据（mzML / .lcd / 谱图 CSV）
- STD 0.005：天然型 SPH20291 对照数据（同上）

### 14.2 代码与运行

| 步骤 | 脚本 | 说明 |
|---|---|---|
| 理论包络 | `theo.py` | 元素表 → 理论同位素峰 |
| 杂质枚举 | `imp.py` | 标记分子 → 20 杂质 / 9 档 |
| 路线 A 主定量 | `同位素杂质计算/run_all.py` | mzML → 四法 9 档结果 + 真实标记杂质表 |
| 路线 A 引擎 | `同位素杂质计算/calc.py` `model.py` `mzml_io.py` | 扣除/校准/读取 |
| 路线 B 解卷积 | `解卷积/deconv_impurity.py` → `deconv.py` | .lcd/.csv → NNLS 9 档结果 |
| LCD 导出 | `lcd2csv.py` | .lcd → 谱图 CSV |

### 14.3 输出文件

- `同位素杂质计算/sp_003_杂质含量.xlsx`：z3/z4 四法 × 9 档杂质明细（每档 1 行合并）、
  9 档汇总、校准实测、四法总览、「真实标记杂质_z3/_z4」表
- `同位素杂质计算/STD_0.005_验证.xlsx`：tier0≈0 验证、天然包络对照、杂质明细(计算值)、
  「真实标记杂质」表（z=3/z=4）
- `同位素杂质计算/汇总报告.md`：结果摘要
- `解卷积/deconv_impurity_output.xlsx`：解卷积路线结果

### 14.4 关键可复核数值

| 项目 | 值 |
|---|---|
| 天然型单同位素质量 | 2823.4015 Da |
| 全标记本体单同位素质量 | 2831.4157 Da |
| 天然型 M0（z=3） | 942.1411 |
| 全标记本体 M0（z=3） | 944.8125 |
| sp_003 tier1 含量（z=3, M0·积分） | 1.669% |
| sp_003 本体含量（z=3, M0·积分） | 98.331% |
| sp_003 真实标记杂质（z=3） | 1.6% |
| STD tier0（套标记引擎） | 0.000% |
| STD 天然包络质量偏移 | +1.98 ppm |

---

## 15. 参考资料

- NIST 同位素数据（经 molmass 库）
- ChemDraw 20.0（分子式与 Exact Mass 参考）
- 岛津 LCMS-9030 仪器规格（分辨率 ~30000 FWHM）

---

*本文档由计算流程自动生成并人工校对。如有疑问，可凭 §14 复现清单逐项复核。*
