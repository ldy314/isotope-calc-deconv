# 同位素计算及解卷积

计算同位素修饰多肽对照品（SPH20291 / SPH20291-Isotope1）的理论同位素分布、同位素取代杂质含量与修饰成功比例（取代率）。**计算引擎统一为 Python（molmass 等开源库）；Excel（`.xlsm`）只做前端，通过按钮调 Python 回填结果。**

> 操作手册与高危坑：`AGENTS.md`　｜　**算法总纲：`ALGORITHMS.md`**　｜　常数与决策台账：`MEMORY.md`　｜　本文件只负责**术语**。
>
> 四条实现路径：① Python CLI `chem.py`（theo / imp / deconv / lcd / jdx）；② `同位素取代率计算/`（富集度，引擎 `embedded_engine.py`；Excel 前端尚未创建）；③ `同位素杂质计算/`（杂质含量，9 档三角扣除 + `同位素杂质计算.xlsm`）；④ Excel 版 `ExactMass_Calculator.xlsx` / `ExactMass_Impurities*.xlsm`。

## Language

**计算引擎（Computation Engine）**:
前20同位素峰的多项式卷积计算统一由 Python 的 **theo 子命令**完成（molmass 库）。Excel 不内嵌任何卷积实现，按钮直接调用 theo 的 API 并回填结果。theo 即唯一实现，无独立核心模块。已定方案（2026-08-06）：方案二 Python 后端。
_Avoid_: VBA 宏、独立 isodist 模块（均已否决）

**同位素种类（Isotope Species）**:
元素列的下拉选项，默认"自然分布"（natural distribution）；选中某纯同位素（如 ¹³C、¹⁵N）时，该列全部原子按该同位素计，不再参与自然丰度卷积。**原子个数的语义随选择变化**：选"自然分布"时输入该元素总原子数（按天然丰度卷积出分布包络）；选纯同位素时输入该同位素的原子数（质量固定 = 个数 × 同位素质量，无分布）。同元素可占多列（如"自然分布 C×120 + ¹³C×6"），支持部分标记多肽。
_Avoid_: 同位素模式、同位素类型

**同位素峰（Isotope Peak）**:
中性分子在特定质量处的同位素变体峰，以相对单同位素峰 M 的偏移标记（M, M+1, M+2, …）。输出时按质量升序排列，给出精确质量与相对丰度。
_Avoid_: 峰簇、谱线

**前20同位素峰（Top-20 Isotope Peaks）**:
输出部分展示的同位素峰数量上限。取**丰度最高的前 20 个峰**，按质量升序排列显示；实际峰数不足 20 时显示全部。输出为**单表 20 行**：行=峰序号，列=中性精确质量 | m/z（带电荷）| 相对丰度(%)。单同位素峰 M 不特殊保留——它是表中质量最小的峰，随丰度排序自然存在；若其丰度排不进前20则不出现。
_Avoid_: 前十个峰（已废弃，2026-08-06 用户改为 20）、M/M+1/M+2 三块布局（已废弃）、强制保留 M 行（已否决）

**相对丰度（Relative Abundance）**:
峰丰度的归一化基准为**最强峰 = 100%**，其余峰按比例显示（质谱国际惯例）。合并后的峰丰度为组内各峰丰度之和。
_Avoid_: 归一化到总和、归一化到 M 峰（均已否决）

**合并峰质量（Merged Peak Mass）**:
两峰因质量差 ≤ 质量精度合并后，显示**丰度加权平均质量**（质谱惯例），反映合并峰的真实质量重心。
_Avoid_: 取组内最小/最强峰质量（均已否决）

**质量精度（Mass Tolerance）**:
用户可配置的峰合并容差，默认 0.001 Da。两峰中性质量差 ≤ 精度时视为同一峰，丰度相加合并；避免 ¹³C 与 ¹⁵N 等近简并峰重复显示。参与计算（合并），不单纯是显示格式。
_Avoid_: 质量容差（同义）、显示精度（含义不同）

**分子式显示（Formula Display）**:
B7 分子式用下标显示原子数，选纯同位素的列以上标质量数标注（如 C₁₂₀¹³C₆H₁₉₈N₂₈O₃₅），一眼看出标记位置与数量。
_Avoid_: 只显示总元素数（丢失标记信息）

**电荷数（Charge z）**:
单个输入值（默认 1，可正可负），前20峰的 m/z 统一用它计算：m/z = (M − z·mₑ)/z。多电荷态列表暂不纳入（理论分布计算不依赖 z，后续可按需增加）。
_Avoid_: 多电荷列表（已暂缓）

**支持元素（Supported Elements）**:
同位素查找表维持 19 种：H, C, N, O, F, Si, P, S, Cl, Br, I, Na, Mg, Al, K, Ca, Fe, Cu, Zn。长表补齐每种元素全部稳定同位素。
_Avoid_: 全元素扩展（已暂缓，数据录入量大）

**工作表布局（Worksheet Layout）**:
Calculator sheet 全新布局：输入区（元素行 / 同位素种类行 / 原子个数行，三行式）+ 参数区（电荷 z、质量精度）+ 单表 20 行输出。原 M/M+1/M+2 三块输出与辅助计算区（count × +1Ratio 等）全部删除，不再保留。
_Avoid_: 新旧输出并存（已否决）

**计算触发（Compute Trigger）**:
输入区旁放醒目按钮"计算前20峰"，点击后调用 theo 子命令（Python）并回填输出表；不设自动重算（Workbook_Change 已否决，避免输入过程中反复调用 Python）。

**元素表输入（Element Table Input）**:
theo 的第五种输入模态（ADR-0006），与 Excel 三行式（元素 / 同位素种类 / 原子个数）一一对应：结构化列表 (element, isotope_or_natural, count)。同元素多列 = 多个条目（如"自然 C×120"与"¹³C×6"并列）。Excel 按钮将三行式直接传给 theo，零转换。亦为未来 deconv / quantify 物种网格的输入结构。
_Avoid_: 拼接带同位素分子式字符串（已否决，往返转换且表达力差）

**修饰成功比例（Labeling Success Ratio）**:
实际质谱图中，达到目标同位素替换数 (k₁_target, …, k_N_target) 的物种（或其质量简并类）占全部标记物种的比例。
_Avoid_: 标记效率（与供应商纯度混淆）、修饰率（歧义）

**物种（Species）**:
由同位素替换列表 (元素, 同位素质量数, 目标数) 的元组数 N 决定的一个 N 维网格点 (k₁, …, k_N)，代表每个替换维度上的实际替换数。
_Avoid_: 组分（chromatography 语境）

**同位素查找表（Isotope Lookup Table）**:
IsotopeData sheet 的角色。每个同位素一行（元素, 质量数, 精确质量, 天然丰度），作为纯参考数据源；选中某同位素时按 (元素, 质量数) 检索其精确质量，该列计算时只计此同位素、忽略该元素其他同位素。选"自然分布"时才取该元素全部同位素做分布卷积。
_Avoid_: 计算表（它不参与计算，只是数据源）

**质量简并类（Mass-Degenerate Class）**:
不同标记组合（物种）因总中性质量差相同而合并为同一质谱峰。合并键为"总中性质量差"，而非 (¹³C, ¹⁵N) 具体组合。
_Avoid_: 等质量峰

**同位素替换（Isotope Substitution）**:
将分子中某元素的指定数量原子替换为指定同位素的操作，输入为三元组 (元素, 同位素质量数, 替换数)。
_Avoid_: 标记、富集（指纯度而非替换数）

**解卷积（Deconvolution / deconv 子命令）**:
读实测高分辨谱（m/z-强度 CSV）→ 用 imp.py 枚举杂质 + theo.py 生成各杂质理论同位素模式（基模式）→ scipy NNLS 拟合（谱 = Σ 权重ᵢ × 模式ᵢ）→ 权重归一化到总和 100% = 各杂质相对含量。支持任意示踪元素数（ADR-0007）。IsoCorrectoR（R 包）仅作单示踪场景交叉验证参照，不作核心引擎。
_Avoid_: 校正（IsoCorrectoR 语境，指扣除天然丰度/示踪剂不纯）

**相对含量（Relative Content）**:
解卷积输出的各杂质占全部标记物种的比例，归一化基准为**总和 = 100%**（组分语义）。区别于理论计算的"相对丰度"（最强峰 = 100%）。
_Avoid_: 相对丰度（归一化基准不同，避免混淆）

**档（Tier）**:
同位素杂质含量计算的分组单位。**tier t = 8 个标记原子中有 t 个被天然同位素替换**（t=0 全标记本体，t=8 全天然形），共 **9 档**。同档内多个杂质（如"少 1 个 ¹³C"与"少 1 个 ¹⁵N"）在 5 ppm 下质量简并（Δm/z < 0.0021 @z=3），包络近乎一致，故**每档合并为 1 行、含量取整档总量**。
_Avoid_: 组、类别（歧义）；按 20 个杂质逐条均分含量（已否决）

**逐级三角扣除（Triangular Subtraction）**:
9 档沿质量轴构成**二对角链**——位置 M0(t) 上只叠两个来源：本档 M0 与更轻一档 t+1 的基峰。M0 法（轻→重，t=8→0）与基峰法（重→轻，t=0→8）逐档扣除，负值截断为 0。**这是本项目区分"不同程度同位素取代"的核心算法**，不用实测谱拟合。
_Avoid_: NNLS 解卷积（对大分子病态，见 ADR-0007）、峰形拟合（已否决）

**TOF 平方标定（TOF Quadratic Calibration）**:
岛津 QTOF `.lcd` 的 `QTFL RawData/Centroid Data` 存值 x 与 m/z 满足 **`m/z = a·x² + c`**，需用已知锚点最小二乘定 a、c。**openszraw 文档的 `mz = x/1e12` 对本变体不适用**（会造成随 m/z 漂移的假峰位）。a、c 依赖仪器/采集模式，换批需重标。
_Avoid_: 线性换算（错误）、固定常数（不可移植）

**自包含引擎（Self-contained Engine）**:
两个交付用 `.xlsm` 的形态——把 Python 引擎源码（`embedded_engine.py`）内嵌到隐藏表 `EngineSrc`，VBA 宏 `RunX` 在运行时提取源码→调用本机 Python 跑→读回 CSV 回填工作表。用户只改「输入」表、点按钮，无需安装任何东西。
_Avoid_: 依赖外部脚本路径、要求用户手动跑 Python

**平均取代率 vs 全标记分子比例（两把尺子，必须分开报）**:
- **平均同位素取代率** = Σ(各物种占比 × 该物种被标记位点数 / 8)，与"修饰成功比例"的经济含义最接近，且**质心法对它严格线性**（档间质心线性插值，偏差 ≤0.005%）。**默认报这个。**
- **全标记分子比例** = 8 个位点全部被标记的分子占比。与上一项的差来自"轻一档"物种。
_Avoid_: 混用（两者的差值可达 1 个百分点以上）

**子命令漂移（Implementation Drift）**:
ADR-0004 定的是 `theo / deconv / quantify` 三个子命令，实际实现是 `theo / imp / deconv / lcd / jdx`（`quantify` 未作为独立子命令，实际由 `同位素取代率计算/`、`同位素杂质计算/` 两个产出线承担）。**以实际实现为准**；ADR 未修订，此处标注漂移。

