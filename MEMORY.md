# MEMORY.md — 事实与决策台账

> 本文件记录**已经定下来的事实、常数与决策**，避免重复讨论、重复计算、重复踩坑。
> 操作手册与陷阱清单在 `AGENTS.md`；**算法总纲在 `ALGORITHMS.md`**；领域术语在 `CONTEXT.md`；架构决策全文在 `docs/adr/`。
> 最后更新：2026-09-24

---

## 1. 已固化的决策（不要再改，除非用户明确要求）

| # | 决策 | 依据 |
|---|---|---|
| D1 | **电荷约定 = ESI 质子加合 `[M+zH]ᶻ⁺`**，`m/z = (M + z·(m_H − m_e))/z`，每电荷净加 1.0072765 u | 早期用电子丢失式，z=2 差约 1 m/z |
| D2 | **理论同位素谱归一化基准 = 最强峰 100%**；而**相对含量/取代率 = 总和 100%** | 两者语义不同，必须分开写 |
| D3 | **峰合并容差默认 0.001 Da**（可配），合并峰质量取**丰度加权平均** | CONTEXT.md |
| D4 | **计算引擎统一 Python**（molmass 卷积），Excel 只做前端 + VBA 调 Python 回填，不内嵌卷积实现 | ADR-0005 |
| D5 | **逐杂质含量不能用 NNLS**（134-C 病态）；改用 **9 档固定系数 + 逐级三角扣除** | ADR-0007 + 合成谱实测（还原和 >200%） |
| D6 | **杂质含量报告值口径 = M0·积分**（四法全列在 Excel，主值取它） | 用户 2026-09-24 拍板"保持现有状态" |
| D7 | **`.lcd` 质量轴 = `m/z = a·x² + c`**，必须用已知锚点标定；禁用 openszraw 的线性换算 | AGENTS.md 坑 #1 |
| D8 | **新批次报告一律复用 `run_all.py` 的参数化生成器**，不自建格式 | 用户 2026-09-23 要求 |
| D9 | 原始数据（`.lcd` 等）与分析产物**不入 git**（沿用既有 `.gitignore` 约定） | 仓库惯例 |
| D10 | git 远端**用 SSH**（`git@github.com:...`），HTTPS 直连被网络拦截 | 2026-09-24 实测 |

---

## 2. 关键常数（可直接引用）

### 2.1 TOF 质量轴标定（20260914 那批 .lcd 实测）
```
m/z = a·x² + c     a = 4.53758732e-04     c = -0.44325      残差 ±10 ppm
```
- 存值 `x` 由 `.lcd` 的 `QTFL RawData/Centroid Data` 里 u64/1e12 得到。
- 用 6 个锚点标定：z=2 tier8 x=1764.742 / z=3 tier8 x=1441.284 / z=4 tier8 x=1248.498 /
  z=2 tier0 x=1767.244 / z=3 tier0 x=1443.324 / z=4 tier0 x=1250.266。
- ⚠️ **a、c 依赖仪器/采集模式**，换批或换模式要重标。

### 2.2 理论单同位素 m/z（`molmass` 复算，质子加合）

| 分子 | M（中和） | 平均分子量 | z=1 | z=2 | z=3 | z=4 |
|---|---|---|---|---|---|---|
| SPH20291（天然） | 2823.4016 | 2825.308 | 2824.4089 | 1412.7081 | 942.1411 | 706.8577 |
| SPH20291-Isotope1（全标记） | 2831.4158 | 2833.250 | 2832.4231 | 1416.7152 | 944.8125 | 708.8612 |

- 天然↔全标记 m/z 差 = **8.0142 / z**（z=2→4.0071、z=3→2.6714、z=4→2.0036）。
- ¹³C 与 ¹⁵N 的位移在 30k 分辨率下**不可分辨**（z=3 差约 0.006 Da）→ 只能说"平均取代率"，不能分元素。

### 2.3 分子式与模型
| 项 | 值 |
|---|---|
| SPH20291 | `C134H198N28O35S2`，13 个残基（序列见 `chemdraw/SPH20291_cdxml解析报告.md`） |
| SPH20291-Isotope1 | `C128[13C]6H198N26[15N]2O35S2`，标记在**第 5 位 Lys(Ac)**，= U-¹³C₆,¹⁵N₂-Lys（SILAC Lys8） |
| 标记原子总数 | **8**（6×¹³C + 2×¹⁵N） |
| 物种数 | (6+1)(2+1) = **21**（含本体） |
| 杂质数 | 21 − 1 = **20** |
| 档数 | **9**（tier 0..8；t = 被天然同位素替换的标记原子数） |

### 2.4 方法参数
- 理论系数窗 **5 ppm**；实测提取/重叠窗 **±0.04 Da**；宽窗找峰心校准范围 **±0.06 Da**。
- `.lcd`/mzML 累积分箱步长：以存值 x 计 `1e-3`（m/z 步长约 1.6e-3 Da）。
- RT 自动窗比例 `frac = 0.1`（超过目标 m/z 窗内最大强度的 10% 的连续 RT 段）。

---

## 3. 各批次结果基准（供回归对照）

### 3.1 sp_003（历史交付批次，产出线 C）
- 数据：`解卷积/再处理2分析/sp_003.lcd`（**已用 TOF 平方标定复核确认 = 全标记形**，z=3 M0 实测 944.805 vs 理论 944.8125）
- 报告值（M0·积分）：**本体 98.33% / tier1 1.67%**，tier 2–8 低于检出。

### 3.2 STD 0.005（天然对照验证）
- 数据：`解卷积/再处理2分析/STD 0.005_001.lcd`（**已复核 = 天然形**，z=3 M0 实测 942.135 vs 理论 942.1411）
- 验证结论：标记引擎 tier0 = **0.000%**（无标记污染，✓）。

### 3.3 20260914 批次（最新，产出线 B + C）
- 数据：`计算数据/20260914 ID of SPH20291/`（STD_002/003/005、SYSPH20291-Isotopel-260911_004、blank_001）
- RT 峰区 **约 260.3–270 s（4.34–4.50 min）**，宽峰（用户口述 4.4 min 一致）；94 扫描。
- 取代率（产出线 B，`解卷积/20260914分析/report.md`）：
  - ISO1_004 = **98.9% ± 1.0%**（质心法三电荷态均值 98.93%，包络序号法 99.77%）
  - STD_002/003/005（天然对照）三电荷态 **|值| ≤ 0.3%** → 零点成立
  - blank_001 三窗内**无峰**
  - z=2 不可靠（窗内噪声占 73.6%），不参与取值
- 杂质含量（产出线 C，**M0·积分**）：
  | 样品 | z | 本体% | tier1% |
  |---|---|---|---|
  | SYSPH20291-Isotope1-260911_004 | 3 | **96.10** | **3.90** |
  | 同上 | 4 | 99.77 | 0.23 |
  | STD_005 | 3 | 标记引擎 tier0 = **0.000** ✓ | — |
  - （口径提醒：同数据若用峰顶法得 97.85 / 2.15；**主值取积分**。）
  - 提取窗 0.02/0.04/0.06 Da 结果一致；与独立报告（2.52%/2.15%）口径不同但方向一致。

### 3.4 历史（Aug）批次
- `WSYSPH20291-260703 MS+_004`：质心法 z=2 → 平均标记度 ≈1.0% → 判为**天然（未标记）品**。
- `WSY_0.05_003`：z=4 校准后富集 ≈ **1.7%** → 天然对照。

---

## 4. ADR 索引（`docs/adr/`）

| 编号 | 决定 |
|---|---|
| 0001 | 物种网格上界 = 目标同位素替换数（不留过度标记余量） |
| 0002 | 标记维度任意 N，由替换列表驱动，不硬编码 (¹³C,¹⁵N) 两维 |
| 0003 | 四种输入模态全部在范围内（分子式 / 多肽序列 / SMILES / ChemDraw） |
| 0004 | CLI 拆为三个子命令 theo / deconv / quantify ⚠️ 实现漂移：实际是 `theo / imp / deconv / lcd / jdx` |
| 0005 | Excel 计算引擎 = Python 后端（molmass 卷积核心） |
| 0006 | theo 新增"元素表"输入模态（三行式，与 Excel 布局一一对应） |
| 0007 | deconv 用自研 NNLS 模式拟合，IsoCorrectoR 仅作交叉验证参照 |
| 0008 | **岛津 QTOF `.lcd` 用 TOF 平方标定读质量轴**（本文件新增） |

---

## 5. 变更史（git，`main` 分支）

| commit | 内容 |
|---|---|
| `452902b` | 补充提交根目录源码与配置（15 文件，历史遗留未跟踪） |
| `0209b10` | `.lcd` 通道接入项目原生引擎；报告生成器参数化 |
| `97bb2b4` | 新增 `.lcd` 直读通道（QTFL 解析 + TOF 平方标定）与批次计算脚本 |
| `e2caa8c` | 修正 M0/基峰法「数学等价」的过强表述（文档升 v1.2，xlsm 已重建） |
| `5375624` | 新增方法学文档《同位素杂质计算方法与原理》 |
| `192ee32` | 真实标记杂质表增加列；新增解卷积程序与 chemdraw 解析 |
| `25ae085` | 经验总结：`.lcd` 读取 + 背景扣除 + docx ChemDraw 分子式提取 |
| `ca6b019` | test_imp_docx：imp.py 枚举 20 杂质与 ChemDraw 参考数据完全一致 |
| `85ac99c` | `.gitignore` 排除参考数据目录 |

远端：`git@github.com:ldy314/isotope-calc-deconv.git`（SSH，走 ssh.github.com:443）。

---

## 6. 历史结论更正记录（**读旧文档时务必对照**）

| 出处 | 原结论 | 更正 |
|---|---|---|
| `docs/经验总结-lcd读取与解卷积.md` §2.4 | "这批 `.lcd` 测的不是 SPH20291（docx 只是参考样例）" | **错**。真因是 openszraw 线性换算。实测：SP 簇 1444.33 的真实 m/z = **944.81**，正是 SPH20291-Isotope1 的 z=3 M0 |
| `解卷积/SPH20291_MS+_解卷积分析.md` §1 | "该 `.lcd` 不含 SPH20291" | **依据不可靠**（同一换算问题）；`.jdx` 结论不受影响。原文件已不在树内，无法复验 |
| `解卷积/再处理2分析/report.md`「已知限制」 | "本批 `.lcd` 不含 SPH20291，以 `.jdx` 为准" | **错**。已实测 `sp_003.lcd` = 全标记形、`STD 0.005_001.lcd` = 天然形 |
| `同位素杂质计算方法与原理.md` v1.1 §7.3/§7.7 | "M0 法与基峰法数学等价" | 已弱化为 v1.2"位置重合、量级一致；链端有固有偏差" |

---

## 7. 待办 / 未决

1. `run_all.py` 的 sp_003 输入路径失效（写死 `解卷积\解卷积\再处理2\sp_003\sp_003.mzML`）。**可修**：改指 `解卷积/再处理2分析/sp_003.lcd` + `STD 0.005_001.lcd`。用户 2026-09-24 明确"先不动"，**改前先问**。
2. `lcd2csv.py` 仍是 openszraw 线性换算（错误）。已加告警注释，未改逻辑（`chem lcd` 仍走它）。
3. `解卷积/deconv_impurity.py`（Aug-8 的 NNLS 版杂质含量脚本）已被 `同位素杂质计算/` 取代，标注为 superseded，未删除。
4. `同位素杂质计算/同位素杂质计算.xlsm` 若需同步文案，须跑 `build_xlsm.py`（Excel COM，**先备份**）。
   ⚠️ 更正（2026-09-24 实测）：**`同位素取代率计算/同位素取代率计算.xlsm` 并不存在**，该目录也没有 `build_xlsm.py` —— 产出线 B 只有引擎源码 `embedded_engine.py`（可用 `--xlsm` 或 `_selftest.py` 直接跑），Excel 前端尚未创建。
5. ADR-0004 与实际实现的子命令不一致（文档层面），未改 ADR，仅在本文件与 `CONTEXT.md` 标注漂移。
6. 根目录 3 个 `ExactMass_Impurities_*.xlsm` 是否入库未定（`_TEST` 与 `_backup_before_deconv` 建议不入库）。
7. **产出线 B `auto_calibrate` 退化 bug**（2026-09-24 复现）：锚点同源（y 值全同）时最小二乘退化 → `a≈1.16e-16`、质量轴塌缩、取代率全 0；`produce_outputs` 无 `a>0`/y 跨度校验，失败 note 说"沿用输入表 a/c"但实际未回退。**绕过**：输入表 B15=否，或 RT 窗放宽到 258–272 s。**修复待用户确认**（计算逻辑改动）。详见 `AGENTS.md` 坑 #10、`ALGORITHMS.md` §4.5。
