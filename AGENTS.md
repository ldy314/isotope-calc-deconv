# AGENTS.md — 给下一个 AI 会话的交接手册

> **读这一份就够。** 本文件的目的只有一个：**不要让下一个会话重走已经走过的弯路。**
> 项目主人的协作习惯：设计先行 → 存疑先问 → 再动手；不做未经验证的语义改动。
>
> 四份交接文档分工：**本文件 = 操作/结构/坑**　｜　`ALGORITHMS.md` = **算法总纲（输入→算法→输出→代码坐标）**　｜　`MEMORY.md` = 常数/决策/结论台账　｜　`CONTEXT.md` = 术语表
> 最后更新：2026-09-24

---

## 0. 这个仓库是干什么的

计算**同位素修饰多肽对照品**（SPH20291 天然型 / SPH20291-Isotope1 全标记型）的：

1. 理论同位素分布与各电荷态 m/z；
2. 同位素取代杂质（未完全标记杂质）含量；
3. 同位素取代率 / 富集度（修饰成功比例）；
4. 岛津 `.lcd` / `.jdx` / `.mzML` / ChemDraw `.cdxml` 的数据读取。

**技术栈**：Python（molmass 做同位素数据、numpy/scipy、olefile 读 OLE 容器、openpyxl/pywin32 做 Excel）。
**Excel 版**：两个自包含 `.xlsm`（内部嵌 Python 引擎 + VBA，用户只改「输入」表点按钮）。

---

## 1. 环境速查（照抄即可）

```bash
PY="C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe"
```

| 项 | 值 |
|---|---|
| **唯一推荐解释器** | `C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe` |
| 已装依赖 | numpy、scipy、molmass、olefile、openpyxl、pywin32(win32com)、openszraw |
| 工作目录 | `D:/code test/chem/同位素计算及解卷积` |
| **git 远端必须用 SSH** | `git@github.com:ldy314/isotope-calc-deconv.git` |
| git 推送通道 | `~/.ssh/config` 已把 `github.com` 映射到 `ssh.github.com:443`；**HTTPS 直连会被拦（`Empty reply from server`），不要改回 HTTPS** |
| 首次 `git push` 失败 | 重试 2–3 次即可（首次连接易超时） |
| Windows/Git Bash 路径 | 传路径给程序用 `D:/...` 正斜杠；`/d/...` 会被 Git Bash 转成 `c:\d\...` |

---

## 2. 目录地图（我要找什么）

```
同位素计算及解卷积/
├── AGENTS.md                    ← 你在读的这份（先读）
├── ALGORITHMS.md                ← 算法总纲：五条链路 输入→算法→输出→代码坐标（想懂算法就看它）
├── MEMORY.md                    ← 事实与决策台账（常数、结论、变更史）
├── CONTEXT.md                   ← 领域术语表（glossary，含 Avoid 项）
├── README_CLI.md                ← chem.py CLI 手册
├── 同位素杂质计算方法与原理.md   ← 杂质含量方法的权威文档（v1.2，供第三方审核）
│
├── chem.py                      ← 统一 CLI 入口（theo/imp/deconv/lcd/jdx）
├── theo.py                      ← 理论同位素分布引擎（molmass，多项式卷积）
├── imp.py                       ← 杂质枚举引擎（笛卡尔积 → 20 个杂质）
├── deconv.py / deconv_excel.py  ← NNLS 解卷积 + 质心法（小分子可用，见坑 #3）
├── formula.py / compounds.json  ← 分子式解析 + 化合物别名库
├── lcd2csv.py / jdx2csv.py      ← .lcd / .jdx → CSV（⚠️ lcd2csv 有坑，见 #1）
├── analyze_remaining.py         ← 「再处理2」批量的质心法批量分析
├── chemdraw/                    ← .cdxml → RDKit 结构解析（另一套工具）
├── docs/
│   ├── adr/0001..0008           ← 架构决策记录
│   └── 经验总结-lcd读取与解卷积.md
│
├── 同位素取代率计算/             ← 【产出线 B】富集度：embedded_engine.py（自包含；⚠️ 无 xlsm 前端）
│                                    verify_autocal.py = 标定门槛回归（已入库，可反复跑）
├── 同位素杂质计算/               ← 【产出线 C】杂质含量：model/calc/lcd_io/run_all + xlsm
├── 解卷积/                       ← 各批次的解卷积分析产物与报告
│   ├── 20260914分析/             ← 最新批次（含 .lcd 正确读法的发现，report.md §3）
│   ├── 再处理2分析/              ← sp_003 批次（⚠️ 该批 .lcd 就在这个目录里）
│   └── SPH20291_MS+_deconv/
├── 杂质列表/                     ← imp 产出的 m/z 清单
├── 参考数据/                     ← 岛津原始数据（.gitignore 排除，不入库）
└── 计算数据/                     ← 批次原始数据（不入库）
```

---

## 3. 三条产出线（用哪条、怎么跑）

### 产出线 A：理论同位素分布 / 杂质枚举（成熟）
```bash
"$PY" chem.py theo SPH20291-Isotope1 --top-n 15
"$PY" chem.py imp  SPH20291-Isotope1
```
算法见 `ALGORITHMS.md` §1 / §2（**偏移不能用排序序号索引**、归一化基准是"最强峰=100%"）。
Excel 版：`ExactMass_Impurities.xlsm`（杂质枚举）。

### 产出线 B：同位素**取代率/富集度**（针对某样品，回答"标记成功多少"）
```bash
"$PY" chem.py deconv SPH20291-Isotope1 --spectrum spec.csv      # 质心法（小分子/NNLS 受限）
"$PY" 同位素取代率计算/embedded_engine.py --xlsm "输入表.xlsm"   # 权威实现：读「输入」表 → %TEMP%\*_*.csv
"$PY" 同位素取代率计算/_selftest.py                             # 离线自测（自动造输入表）
"$PY" 同位素取代率计算/verify_autocal.py                         # 标定门槛回归（应全 PASS）
```
批量/权威实现：`同位素取代率计算/embedded_engine.py`（自包含；**质心法 + 包络序号法**）。算法见 `ALGORITHMS.md` §4。
- ⚠️ **本目录没有 `.xlsm`、也没有 `build_xlsm.py`**（Excel 前端尚未创建）。引擎可直接跑：输入表布局见该文件头注释；`_selftest.py` 会自动生成一张。
> 最新批次（20260914）的结论见 `解卷积/20260914分析/report.md`：ISO1_004 取代率 **98.9% ± 1.0%**。

### 产出线 C：同位素**杂质含量**（回答"未完全标记杂质占多少"，**当前主战场**）
```bash
cd "同位素杂质计算"
"$PY" run_all.py          # 原生 sp_003 / STD 0.005 流程（⚠️ 输入 mzML 路径已失效）
"$PY" run_lcd_impurity.py # .lcd 批次入口（TOF 标定 + 读谱 + 调 run_all 生成器出报告）
```
- 方法权威文档：`同位素杂质计算方法与原理.md`（v1.2）；**算法总纲见 `ALGORITHMS.md` §3**
- Excel 版：`同位素杂质计算.xlsm`（内嵌引擎，需 `build_xlsm.py` 重建；重建前先备份）
- **报告格式一律复用 `run_all.py` 里已参数化的生成器**：
  `write_sp003_workbook(sample_label=, out_path=, zs=, info_overrides=)` /
  `write_std_workbook(std_label=, out_path=, extra_z_res=)`。
  **不要自建新格式**——曾经自建过一次，被要求全部推翻重做。

---

## 4. ⚠️ 高危坑（**先读这一节，能省一整天**）

### 坑 #1（最贵的一个）岛津 QTOF `.lcd` 的质量轴**不是线性的**
`QTFL RawData/Centroid Data` 里存的 `x` 与真实 m/z 是**平方关系**：

```
m/z = a·x² + c        a ≈ 4.53758732e-04，c ≈ −0.44325
```

- **`openszraw` 文档写的 `mz = x/1e12` 对本变体不适用**（`lcd2csv.py` 正是这么干的）。
- 线性换算会把真实 **942.15 显示成 1442**、**944.81 显示成 1444**，而且偏差随 m/z 漂移 → 一度让人得出"这批数据不含 SPH20291"的**错误结论**（该结论已在多处就地更正）。
- `a, c` 依赖仪器/模式，**必须用已知锚点最小二乘标定**（用天然形/全标记形的 z=2/3/4 M0 峰位做锚点，典型残差 ±10 ppm）。不同批次若换了模式要重新标定。
- **正确工具**：`同位素杂质计算/lcd_io.py`（`set_calibration(a,c)` → `read_lcd(...)`）或 `同位素取代率计算/embedded_engine.py`。
- 自检方法：标定后看 z=3 的 M0 是否落在 **天然 942.141 / 全标记 944.813**（±0.01 Da）。

> **这条坑已经污染了 3 份历史文档的结论**，读到它们时务必对照本节：
> `docs/经验总结-lcd读取与解卷积.md` §2.4、`解卷积/SPH20291_MS+_解卷积分析.md` §1、
> `解卷积/再处理2分析/report.md`「已知限制」。这三处已加更正说明。

### 坑 #2 别把 `.lcd` 当成打不开的二进制
`.lcd` 就是 **OLE 复合文档**，用 `olefile` 直接读：
- 质心数据：`QTFL RawData/Centroid Index`（24 B/记录）+ `QTFL RawData/Centroid Data`；
- 扫描头 72 B：`+4` RT(**毫秒**)、`+24` payload 字节数、`+36` 强度宽度 `w∈{1,2,4}`；
- `payload = N×u64(质量轴) + N×w(强度)`，`N = payload/(8+w)`。
- **RT 单位是秒**（用户常用分钟，4.4 min = 264 s）。
- 方法文件 `.lcm`/`.lcb` 里**没有**化合物条目（全扫描采集），别指望从数据里读目标分子式。

### 坑 #3 134 碳分子上 **NNLS 逐杂质解卷积是病态的，不可信**
- 21 个同位素态的理论包络都被 128 个天然碳的细结构主导、彼此近乎共线 → 解非唯一，合成谱还原测试的相对含量之和 **> 200%**（物理上不可能）。
- 所以**"逐杂质含量"不能用 NNLS**，本项目的做法是 **9 档固定系数 + 逐级三角扣除**（见产出线 C）。
- 质心法（强度加权质心在两理论质心间线性插值）对包络重叠不敏感，**对"平均取代率"严格线性**，是这类大分子的稳健选择。

### 坑 #4 「积分」与「峰顶」的偏向**随数据集反转**，不能照搬结论
机理：数据是质心谱，逐扫描峰位抖动 → 累加分箱后**一个真实峰被展开成多个相邻箱**。
`积分 = 该峰离子总数（强度守恒，物理正确）`；`峰顶 = 单个箱的强度`（被抖动分散度除小）→ **哪个档抖动大，它就被相对低估**。

| 数据 | tier0 积分/峰顶 | tier1 积分/峰顶 | 结果 |
|---|---|---|---|
| sp_003（mzML） | 4.78 | 2.20 | tier1 抖动小 → **峰顶偏高** |
| 20260914（.lcd） | 2.92 | 5.63 | tier1 抖动大 → **峰顶偏低** |

**项目约定：报告值取 M0·积分**（四法结果都列在 Excel 里）。
（曾经据 sp_003 的规律把「峰顶偏高」当普适规律并以其为主值，是**错的**。）

### 坑 #5 RT 窗口必须**覆盖整峰**
这批色谱峰是宽峰（约 260.3 s 起、尾部拖到 270 s）。用窄窗 261–266 s（仅 78% 峰面积）会把 tier1 从 **3.90% 漂到 5.01%**。原生流程用 `frac=0.1` 自动检测窗口，别手改成窄窗。

### 坑 #6 电荷约定是 **ESI 质子加合 `[M+zH]ᶻ⁺`**
`m/z = (M + z·(m_H − m_e))/z`，每电荷净加 **1.0072765 u**。**不是** `[M]ᶻ⁺`（电子丢失）——早期版本用错了，z=2 会差约 1 m/z。
关键理论值（已用 `molmass` 复算，2026-09-24）：

| 分子 | M（单同位素中和） | z=1 | z=2 | z=3 | z=4 |
|---|---|---|---|---|---|
| SPH20291（天然） | 2823.4016 | 2824.4089 | **1412.7081** | **942.1411** | 706.8577 |
| SPH20291-Isotope1（全标记） | 2831.4158 | 2832.4231 | **1416.7152** | **944.8125** | **708.8612** |

两者的 m/z 差 = 8.0142/z（z=2→4.0071、z=3→2.6714、z=4→2.0036）。

### 坑 #7 `run_all.py` 的输入路径是失效的
`run_all.py` 里写死 `解卷积\解卷积\再处理2\sp_003\sp_003.mzML`（目录已不存在，且**全树无任何 .mzML**）。
**但 sp_003 那批的真实数据还在**：`解卷积/再处理2分析/sp_003.lcd` 与 `STD 0.005_001.lcd`（已用 TOF 平方标定复核确认：sp_003 = 全标记形、STD = 天然形）。
→ 要复活这条线，把输入改指向这两个 `.lcd` 即可（用户 2026-09-24 明确说"这一项先不动"，**改前先问**）。

### 坑 #8 不要重新跑一遍已知的答案
`sp_003` 报告值（M0·积分）：本体 **98.33%** / tier1 **1.67%**。
`STD 0.005` 验证：标记引擎 tier0 = **0.000%**（天然对照无标记污染，✓）。
20260914 批次：ISO1_004 z=3 本体 **96.10%** / tier1 **3.90%**（M0·积分口径）。

### 坑 #9 Excel 相关
- 重建 `.xlsm` 需 **Excel COM**（已可用，16.0）。`build_xlsm.py` 会**覆盖交付用的 xlsm** → **先备份**（已有先例 `同位素杂质计算_backup_20260923.xlsm`）。
- 目录里 `~$*.xlsx` 是 Excel 锁文件（工作簿正被打开），关闭 Excel 后自动消失，别当垃圾删。
- 交付前核对内嵌引擎是否同步：`EngineSrc` 表内容 + 「方法说明」页文案。

---

### 坑 #10（2026-09-24 发现，**2026-09-25 已修**）`auto_calibrate` 在「锚点同源」时退化，把质量轴算塌

**触发**：产出线 B 开自动标定（输入表 B15 = 是），且**锚点全部来自同一参照形、同一电荷态**。
→ 3 个锚点的理论 y 值完全相同 → 最小二乘**退化解** **`a ≈ 1.16e-16`、`c ≈ 942.475`**（质量轴塌缩：所有 x 都算成 942.5）→ 取代率全 **0.00**、判定全「—」，且 z=2/z=4 的「用峰数」也一起归零。

**放大因素**：
1. 窄 RT 窗（261–266 s）下只有 z=3 找得到锚点（z=2/z=4 的 `_apex_x` 落空）→ 3 个 STD 给出 3 个 y 相同的锚点；
2. `produce_outputs` 只检查 `len(anchors) >= 3` 就采纳，**没有 `a > 0` / y 跨度的 sanity 校验**；
3. 失败时的 note 写「沿用输入表 a/c」，但代码**并未回退**，仍用塌缩后的 (a, c) —— 文案误导。

**✅ 修复内容**（`embedded_engine.py`，2026-09-25）：

| 位置 | 改动 |
|---|---|
| 常量区 | 新增三个门槛：`MIN_ANCHOR_SPAN = 0.05` Da、`MIN_CAL_A = 1e-8`、`MAX_CAL_REL = 2e-4`（200 ppm） |
| `auto_calibrate` | 拟合前**校验①锚点理论质量跨度**（防同源退化）；拟合后**校验②`a > MIN_CAL_A` 且有限**（防塌缩）、**校验③残差 ≤ `MAX_CAL_REL`**（防误配）。任一不过 → `anchors = []` + 具体原因 note |
| `auto_calibrate` | 修 `anchors = pts` 的登记时机 —— 原先放在收敛 `break` **之后**，导致「首轮即收敛」时诊断误报 `n_anchor=0`（数值无害但掩盖真实通过） |
| `produce_outputs` | 采纳条件加二次防御：`len(anchors) >= 3` **且** `a2 > MIN_CAL_A` 且有限；否则 `anchors = []` —— **真回退**到输入表 a/c，并修正 note 文案 |

**门槛为何这样定**：
- `MIN_ANCHOR_SPAN`：同源锚点理论值全同（跨度 0）时，设计矩阵 `[x², 1]` 两列**不可分辨**，拟合结果由数据无关的伪逆方向决定 —— 有时侥幸接近真值、有时塌缩，**必须拒绝**。
- `MAX_CAL_REL = 200 ppm`：实测正常残差仅 ±10 ppm；而锚点误配（错认同位素峰）至少偏 1 个同位素间距（z=3 → 0.334 Da @942 ≈ 355 ppm）→ 200 ppm 能拦下误配，同时留 20× 正常余量。

**回归验证**：

| 用例 | 修前 | 修后 |
|---|---|---|
| 真实数据 auto_cal=是 + 窄窗 | a=1.157e-16 → **0.00（错）** | 拒绝并回退 → a=4.53758732e-04，**99.00** ✓ |
| 真实数据 auto_cal=是 + 宽窗 | a=4.53541535e-04（侥幸） | 同样拒绝并回退 → **99.06** ✓ |
| `_selftest.py` 端到端 | 全 0、z=2/z=4 用峰 0 | status=OK；SP 98.34–99.46、3 个 STD −0.20~+0.28、blank 无峰 ✓ |
| 合成谱定向测试（4 用例 + 1 边界） | — | **全部 PASS** |

四配置结果**收敛一致**（修前 0.00 / 99.00 / 99.06 / 99.01 → 修后 99.00 / 99.00 / 99.06 / 99.06）。

**注意（保持有效）**：自动重标定是**精修而非盲搜** —— 输入表 a/c 须已准到让峰搜索窗（±0.5 Da）能命中，否则第 1 轮就 0 锚点并安全拒绝（见 §8 回归脚本 T2b）。故**常规仍建议 B15=否 + 输入已知 a/c**；B15=是 只在换仪器/换批次后需要重标时使用。

> 已交付的取代率 **98.9%** 来自独立脚本 `解卷积/20260914分析/analyze_20260914.py`（自带 `calibrate()`，不调用本函数）→ **本来就不受此 bug 影响**，本轮未改动其数值。

---

## 5. 术语速查（详见 `CONTEXT.md`）

| 词 | 含义 |
|---|---|
| **档 / tier t** | 8 个标记原子中有 t 个被天然同位素替换；t=0 全标记本体，t=8 全天然形，共 **9 档** |
| **物种 / species** | (¹³C 替换数, ¹⁵N 替换数) 网格点；共 (6+1)(2+1) = 21 个（含本体） |
| **杂质** | 21 个物种减去输入本体 = **20 个**（笛卡尔积枚举，含全天然形） |
| **相对丰度** | 理论谱里的归一化，**最强峰 = 100%** |
| **相对含量 / 取代率** | 组成占比，**总和 = 100%**（与上一条基准不同，别混用） |
| **质心法** | 强度加权平均 m/z，在天然↔全标记理论质心间线性插值 → 平均取代率 |
| **包络序号法** | 按同位素序号 `M0_nat + j·u` 逐峰比对，反推各档丰度（独立交叉验证） |

---

## 6. 数据资产（原始数据不入库，但别删）

| 位置 | 内容 |
|---|---|
| `计算数据/20260914 ID of SPH20291/` | 最新批次：STD_002/003/005、SYSPH20291-Isotopel-260911_004、blank_001（`.lcd`）+ 方法文件 + **本批交付报告** |
| `解卷积/再处理2分析/` | sp_003.lcd、STD 0.005_001.lcd（**产出线 C 的历史数据**） |
| `参考数据/` | 更早的岛津原始数据（`.gitignore` 排除） |
| `杂质列表/SPH20291-Isotope1/` | imp 产出的 z=2/z=3 杂质 m/z 清单 |

**已入库的例外**（源码/报告，非数据）：`解卷积/` 下 3 份分析报告 `.md` + `analyze_20260914.py`、`同位素取代率计算/embedded_engine.py`（2026-09-24 补入，避免新会话引用落空）。

**仍未入库**（按项目约定）：`计算数据/`、`参考数据/`、`杂质列表/`、`同位素取代率计算/` 下的 `_*.py` 与 `_selftest_input.xlsx`（`_*` 命中 `.gitignore`）、各批次谱数据与中间 CSV、`*.xlsm` 备份。

---

## 7. 当前状态与未决事项

- ✅ 产出线 A（理论/杂质枚举）成熟稳定。
- ✅ 产出线 B（取代率）：20260914 批次已出报告（98.9% ± 1.0%）。
- ✅ 产出线 C（杂质含量）：20260914 批次已按原生格式交付
  （`计算数据/20260914 ID of SPH20291/` 下 `SYSPH20291-Isotope1-260911_004_杂质含量.xlsx` + `STD_005_验证.xlsx` + `汇总报告.md`）；
  口径 = **M0·积分**（本体 96.10% / tier1 3.90%）。旧的峰顶口径产物已删除，**不要重新生成两套口径并存**。
- ⏳ 未决：`run_all.py` 的 sp_003 输入路径（用户明确"先不动"）；`lcd2csv.py` 仍是错误的线性换算（仅作历史参考，勿用）。
- ✅ **已解决（2026-09-25）**：产出线 B 的 `auto_calibrate` 退化 bug（见坑 #10）—— 已加三道采纳门槛 + 真回退 + 修正 note，并通过真实数据与合成谱双向回归。
- ⏳ 未决：根目录 3 个 `ExactMass_Impurities_*.xlsm` 是否入库。
- 📌 **改任何东西前先问用户**（本项目主人的明确偏好）。改动计算逻辑属"语义改动"，必须经确认。

---

## 8. 修改纪律

1. **设计先行、存疑先问**：有歧义/涉及业务判断的，先给选项让用户拍板，别自作主张。
2. **改前备份**：会覆盖交付物（`.xlsm`、报告）的操作，先备份。
3. **改后回归**：改计算链路后必须跑三个回归 ——
   - `同位素取代率计算/verify_autocal.py`（标定门槛，合成谱定向测试，全 PASS 退出码 0）
   - `同位素杂质计算/model.py`（理论层基准：20 杂质 / 9 档 / z=3 tier0 M0=944.8125）
   - 合成谱闭环（把已知答案喂回扣除引擎，看能否还原 —— 见 §10）
   外加全仓残留搜索（改文案时）。
4. **提交**：走 SSH；commit message 说清"改了什么/为什么"；原始数据与分析产物不入库。
5. **文档同步**：方法学文档、`EngineSrc` 内嵌引擎、说明页文案三者要一致（曾经只改了两处就交付）。

---

## 9. 弯路档案（发生过的误判，别再犯）

| 时间 | 误判 | 真因 | 现状 |
|---|---|---|---|
| 2026-08-07 | "这批 `.lcd` 不含 SPH20291，是另一物质" | `openszraw` 线性换算 → 1442/1444 假峰位 | **已更正**（真值 942.14/944.81） |
| 2026-08-07 | "NNLS 可以给逐杂质含量" | 134-C 包络高度重叠，解非唯一 | 改走质心法 / 9 档三角扣除 |
| 2026-08-10 | "峰顶比积分准"（据 sp_003 归纳） | 偏向方向随数据抖动反转 | 约定取 **M0·积分** |
| 2026-09-23 | "用窄窗 261–266 s 取峰" | 宽峰被截断 78% 面积 | 用自动窗覆盖整峰 |
| 2026-09-23 | "`M0` 法与基峰法数学等价" | 链端无对偶档抵消，有固有偏差 | 文档 v1.2 已弱化为"位置重合、量级一致" |
| 2026-09-23 | 自建了一套简化 Excel 格式 | 项目有原生参数化生成器 | 已推翻，一律复用 `run_all.py` 生成器 |

---

## 10. 最小复现清单（验证环境是否正常）

```bash
cd "D:/code test/chem/同位素计算及解卷积"
PY="C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe"

"$PY" 同位素杂质计算/model.py                    # 应打印 20 杂质 / 9 档，z=3 tier0 M0=944.8125
"$PY" chem.py theo SPH20291 --top-n 5            # 理论谱
"$PY" chem.py imp  SPH20291-Isotope1 | head -5   # 应枚举 20 个杂质
"$PY" 同位素取代率计算/verify_autocal.py         # 标定门槛回归：应"全部 PASS"（退出码 0）
git remote -v                                    # 应显示 git@github.com:ldy314/...
```

跑通这几条 = 环境与认知都对上了，可以开工。
