# ALGORITHMS.md — 算法总纲

> **用途**：让一个全新的 AI 会话**不读源码**也能准确掌握本项目的**算法**（输入 → 算法 → 输出 → 代码坐标）。
> 三份文档分工：**结构/操作/坑 → `AGENTS.md`**　｜　**常数/决策/结论 → `MEMORY.md`**　｜　**术语 → `CONTEXT.md`**
> 杂质含量的第三方可审核版方法学全文：`同位素杂质计算方法与原理.md`
> 最后更新：2026-09-24

---

## 0. 五条链路一览

| # | 链路 | 回答什么问题 | 入口 | 算法族 |
|---|---|---|---|---|
| **A** | 理论同位素分布 | 给定分子式 + 电荷，理论谱长什么样 | `theo.py` | 多项式卷积 + 容差分箱 |
| **B** | 杂质枚举 | 这个标记分子一共可能有多少种同位素取代杂质 | `imp.py` | 修饰列笛卡尔积 |
| **C** | **同位素杂质含量** | 未完全标记的杂质各占多少 | `同位素杂质计算/` | **9 档固定系数 + 逐级三角扣除**（解析法，非拟合） |
| **D** | **取代率 / 富集度** | 标记成功了多少（一个百分数） | `同位素取代率计算/` | **质心法** + **包络序号法**（交叉验证） |
| **E** | 数据读取与质量轴 | 原始谱怎么变成 (m/z, 强度) | `lcd_io.py` / `mzml_io.py` / `jdx2csv.py` | OLE 解析 + **TOF 平方标定** |
| F | 结构解析（旁支） | 从 ChemDraw 拿分子式/序列 | `chemdraw/` | CDXML XML 解析 + RDKit |

**一句话区分 C 与 D（最容易被混淆的一点）**：
- **C 是「分布」问题** —— 用**理论固定系数**把 9 个档逐级扣开，得到**各档占比**；输出多行（每档一行）。
- **D 是「位置」问题** —— 只看实测包络的**质心落在天然形与全标记形之间的哪个位置**，得到一个**平均取代率**；输出一个百分数。
- 两者**口径不同，不可直接互换**（见 §4.3「两把尺子」）。

---

## 1. 链路 A：理论同位素分布（`theo.py`）

**输入**：元素列 `(element, isotope_or_natural, count)`，例如 SPH20291-Isotope1 =
`[("C","natural",128), ("C","13",6), ("H","natural",198), ("N","natural",26), ("N","15",2), ("O","natural",35), ("S","natural",2)]`

**算法**：
1. **单列分布**：`natural` 列取该元素全部稳定同位素 (质量, 天然丰度)（数据源 `molmass.ELEMENTS`，即 NIST）；纯同位素列（如 `13`）为单点分布。然后**把单原子分布自卷积 count 次**（动态规划累积 `(质量偏移, 概率)`）→ 即「多项式展开」。
2. **逐列卷积**：所有元素列依次卷积；**每次卷积后按容差 `tol`（默认 0.001 Da）聚类合并**质量差 ≤ tol 的峰：**丰度相加、质量取丰度加权平均**；剪除概率 < `min_prob` 的峰。
3. **加合离子换算**（ESI 惯例）：
   ```
   m/z = (M + z·(m_H − m_e)) / z        m_H = 1.00782503223,  m_e = 0.000548579909
   ```
   每电荷净加 **1.0072765 u**。`z=0` 表示中性（m/z = M）。**z>0 为 `[M+zH]ᶻ⁺` 质子加合，不是 `[M]ᶻ⁺` 电子丢失式**（早期版本错过，z=2 差约 1 m/z）。
4. **归一化 + 截取**：相对丰度以**最强峰 = 100%**；显示相对丰度 ≥ `min_ab`（默认 0.05%）的**前 `top_n` 个峰**，**按质量升序**输出。

**输出**：每峰 `中性精确质量 | m/z | 相对丰度%`。

**⚠️ 归一化基准**：理论谱是「最强峰 = 100%」；而相对含量/取代率是「总和 = 100%」。**两者语义不同，不可混用**。
**⚠️ 偏移不能用「排序序号」索引**：标记分子的 M+1 会因 ¹³C(+1.00335) 与 ¹⁵N(+0.99703) 拆成两个相邻峰，序号会错位。必须用「落在某锚点窗口内的**全部峰的丰度之和**」（见 §3.1）。

**代码坐标**：`theo.py::compute_theoretical_spectrum` / `_column_distribution` / `_convolve_columns` / `_merge_by_tolerance`

---

## 2. 链路 B：杂质枚举（`imp.py`）

**输入**：与 A 相同的元素列（修饰态分子）。

**规则**：
1. **修饰列** = 同位素种类**不是** `natural` 的列（如 `("C","13",6)`、`("N","15",2)`）。
2. 对每个修饰列 `(E, iso, n)`：把其中 **k 个**修饰原子替换回该元素的天然同位素，`k = 1..n`（`k=n` 即该列全天然）。**`k=0` 是输入本身，不输出。**
3. **`natural` 列原样保留**在每个杂质里，不参与枚举（即「天然同位素自身的杂质」不列举）。
4. 多个修饰列做**笛卡尔积**，排除**所有列同时 k=0** 的组合（= 输入本身）。

**本项目的实例**：修饰列 = ¹³C×6、¹⁵N×2 →
```
(6+1)(2+1) − 1 = 20 个杂质
```
其中含 1 个「全天然形」（8 个标记原子全被替换）= SPH20291 本身。

**输出**：三行式元素表 + 分子式平文名（`C2H3` 这种）。

**代码坐标**：`imp.py::enumerate_impurities`（返回 `replaced_total` = 被替换的标记原子总数，用于 C 线的档划分）

---

## 3. 链路 C：同位素杂质含量 —— 9 档固定系数 + 逐级三角扣除 ★核心

> **本质**：这是**解析算法，不是实测谱解卷积拟合**。全部系数来自 `theo.py` 的理论包络，**不含任何实测谱信息**；实测谱只用来提供各锚点位置的**强度**。

### 3.1 模型层（`model.py`）—— 把 20 个杂质归成 9 档

- **档定义**：`tier t` = 8 个标记原子中有 **t 个被天然同位素替换**（`t=0` = 全标记本体，`t=8` = 全天然形）→ 共 **9 档**。20 个杂质按 `replaced_total` 归入 t=1..8。
- **同档合并的理由**：同档内不同组合（如「少 1 个 ¹³C」vs「少 1 个 ¹⁵N」）在 5 ppm 下**质量简并**（Δm/z < 0.0021 @z=3），实验上同一峰、无法分辨 → **每档合并为 1 行，含量取整档总量**（不再均分给成员）。
- **每档代表包络**：对该档全部成员的 theo 峰列表按索引对齐后**取算术平均**（m/z 与概率分别平均）；锚点取
  - `m0_mz` = 成员 M0 峰位的均值（单同位素峰）
  - `base_mz` = 成员**基峰**（概率最大的单峰）峰位的均值
- **关键系数 = 窗口重叠概率**（不是按序号取的相对丰度）：
  ```
  window_overlap(peaks, center, ppm) = Σ pr   for all (mz, pr) with |mz − center| ≤ center·ppm/1e6/2
  ```
  即 **5 ppm 全宽窗内全部峰的概率之和**（±2.5 ppm）。这样 ¹³C/¹⁵N 拆出的双峰能被完整计入。
- 每个档 × 每个电荷态（**z = 2 / 3 / 4**）各存一套 `m0_mz, base_mz, peaks[(mz, 概率)]`，概率已归一化到「该组成保留峰概率之和 = 1」。

**自检**：`python 同位素杂质计算/model.py` → z=3 应有 tier0 M0 = **944.8125**、tier8 = **942.1411**；z=4 tier8 = **706.8577**。

### 3.2 谱图层（`lcd_io.py` / `mzml_io.py`）—— 质心谱 → 轮廓谱

1. 读该 RT 窗口内**全部扫描**；
2. **自动检出 RT 窗口**：逐扫描统计目标 m/z 窗内的总强度，取超过最大值 **`frac=0.1`** 比例的**连续 RT 段**；
3. **分箱累加**：按质量轴步长 **0.001**（`.lcd` 按存值 x 计）**把全部扫描的峰累加**成一条轮廓谱。

**⚠️ 为什么必须分箱累加，而不能按索引相加**：数据是**质心谱**，逐扫描峰位有抖动（同一化合物每张扫描的峰位不固定）→ 累加后**一个真实峰被展开成多个相邻箱**。这直接决定了 §3.4「积分 vs 峰顶」的偏向（见 AGENTS.md 坑 #4）。

**⚠️ RT 窗口必须覆盖整峰**：峰是宽峰（20260914 那批约 260.3 s 起、拖尾到 270 s）。窄窗（261–266 s，仅 78% 面积）会把 tier1 从 **3.90% 漂到 5.01%**。别手改窄窗。

### 3.3 质量校准（`calc.py::calibrate`）

- 建 **18 个锚点** = 9 档 × {M0, 基峰}；
- 每个锚点在其理论位置上 **±0.06 Da（`SEARCH_HALF`）** 内取**最强信号点**作为**真实峰心**；
- 校准残差（真实峰心 − 理论值，ppm）写入报告的「校准与实测」表 —— 正常情况下应在 ±10 ppm 量级。

### 3.4 提强度（`calc.py::extract_measured`）

在真实峰心 **±0.04 Da（`MEAS_HALF`）** 窗内取两种强度：

| 方式 | 定义 | 物理含义 |
|---|---|---|
| **积分** | `Σ(窗内强度) − 基线 × 箱数` | **该峰的离子总数**（强度守恒，物理正确） |
| **峰顶** | `max(窗内强度) − 基线` | 单个箱的强度 |

- **基线** = 测量窗外侧环（`[center+0.05, center+gap/2−0.005]`，`gap` = 相邻档 M0 间距）的中位强度，保证环内不含相邻档峰。质心谱环内常无数据点 → 基线 = 0（本底干净，结果与未扣一致）。
- **`MEAS_HALF=0.04 Da` 的选取**：足以容纳整峰，又不会串入相邻档（相邻档相距 ≈ 0.33/z Da）。

### 3.5 扣除（**算法核心**，`calc.py::deduce_m0` / `deduce_base`）

**重叠矩阵**：
```
W[i][j] = tier j 的全部理论峰落在 tier i 锚点 ±0.04 Da 内的概率之和
```

**二对角链结构**（关键洞察）：9 档沿质量轴排列，位置 `M0(t)` 上**只叠两个来源**——
本档 t 的 M0，以及**更轻一档 t+1 的基峰**（因 5 ppm 下 `基峰(t+1) ≡ M0(t)`）。
非相邻档相距 ≈ 0.33/z Da，**远在 0.04 Da 窗外，不会串入**。

**M0 法（轻 → 重，t = 8 → 0）**：
```
A[t] = ( I_M0[t] − A[t+1] · W[t][t+1] ) / W[t][t]        负值截断为 0
```
**基峰法（重 → 轻，t = 0 → 8）**：
```
A[t] = ( I_base[t] − A[t−1] · W[t][t−1] ) / W[t][t]      负值截断为 0
```

**⚠️ 两法并非严格等价**（文档 v1.1 曾误述为「数学等价」，v1.2 已更正）：5 ppm 下二者**位置重合、量级一致**，但**链端档位没有对偶档可抵消**，扣除不闭合，存在固有偏差。合成谱闭环验证（设定本体 98.3% / tier1 1.7%）：M0·积分精确还原 **98.300 / 1.700**；基峰法给 tier1 ≈ 0.73%、tier2 ≈ 0.62%（设定 tier2 = 0）。

### 3.6 归一化与四种估计量

```
含量 % = A[t] / Σ A × 100        （总和 = 100%）
```
四种估计量 = {M0, 基峰} × {积分, 峰顶} → `m0_integral` / `m0_top` / `base_integral` / `base_top`，**全部并列写在 Excel 里**。

> **报告值口径 = `M0·积分`**（项目约定，用户 2026-09-24 拍板）。**不要只报一个隐含口径**，四法要并排给。

**积分 / 峰顶 谁偏高，随数据集反转**（不能照搬结论）：

| 数据 | tier0 积分/峰顶 | tier1 积分/峰顶 | 结果 |
|---|---|---|---|
| sp_003（mzML） | 4.78 | 2.20 | tier1 抖动小 → **峰顶偏高** |
| 20260914（.lcd） | 2.92 | 5.63 | tier1 抖动大 → **峰顶偏低** |

### 3.7 独立验证表（排除本体包络串扰）

另用「本体理论同位素包络的最小二乘振幅 A」**预测**各锚点处应有的天然同位素峰强度，**实测 − 预测 = 残余**：
- **残余 = 真实标记杂质**（扣掉了本体自身同位素包络的投影）；
- 天然型对照（STD）作**阴性对照**：标记引擎 tier0 应为 **0.000%**。

### 3.8 算法边界：为什么这里**不用** NNLS 解卷积

134 碳分子的 21 个同位素态理论包络被 128 个天然碳的细结构主导、彼此近乎共线 → **解非唯一**（合成谱还原的相对含量之和 > 200%，物理上不可能）。
→ **逐杂质含量必须走本节的 9 档三角扣除**；NNLS（`deconv.py`，ADR-0007）只对小分子（碳数 ≤ 50）可信。

**代码坐标**：`model.py::build_model/tier_peaks/window_overlap`　`calc.py::calibrate/extract_measured/_overlap_matrix/deduce_m0/deduce_base/normalize_amounts/analyze_profile`　编排与报告：`run_all.py`（参数化生成器）、`run_lcd_impurity.py`（`.lcd` 批次入口）

---

## 4. 链路 D：同位素取代率 / 富集度

**输入**：`.lcd`（或已标定好的谱）+ 天然形分子式 + 全标记形分子式 + 电荷态列表。
**预处理**：与 C 共用 —— TOF 平方标定 → RT 自动窗 → 按 x 分箱累加。

### 4.1 质心法（主，ADR-0007）

在**完整覆盖两套包络**的窗口内：
```
窗口 = [ M0_nat − 0.5u ,  M0_lab + 8u ]        u = 一个同位素间距（13C 间距 1.0033548）
```
1. **扣基座**：窗内强度减去 **p25 分位数**（`BASE_PCT=25`）；
2. **阈值**：低于 `0.5% × 窗内最大值`（`THR_FRAC=0.005`）的箱置零（窗内以质心噪声峰为主，z=2 时噪声占 ~70%）；
3. **强度加权质心**：`m_meas = Σ(v·m) / Σv`；
4. **换算取代率**：
   ```
   取代率_raw = (m_meas − m_nat) / Δ × 100%        Δ = m_lab − m_nat
   ```
5. **零点校准**：用**天然对照样品**（默认 `STD_002, STD_003, STD_005`）在同电荷态下的质心偏移 **δ** 作零点：
   ```
   取代率 = (m_meas − δ − m_nat) / Δ × 100%
   ```

**为什么质心法对大分子稳健**：它只看「质心落在两理论质心之间的哪个位置」，**对包络重叠不敏感**；且对**平均取代率严格线性**（档间质心线性插值，偏差 ≤ 0.005%）。

### 4.2 包络序号法（独立交叉验证）

```
I[j] = 在 m = M0_nat + j·u 处 ±0.2u 内的最大强度      j = 0..24
j0   = 首个满足 I[j] ≥ 5% · max(I) 的序号            → 主包络起点
       j0 = 0 → 主形为天然形；  j0 = 8 → 主形为全标记形
w    = I[j0 .. j0+8] / max(I)                        → 主形形状模板（8 个序号 = 标记原子数）
```
1. **求主形振幅**：用形状模板 `w` 对 `I[j0+k]` 做最小二乘定标 → `A0`；
2. **求邻档占比**：`f_m1 = I[j0−1] / w[0] / A0`、`f_m2 = I[j0−2] / w[0] / A0`（轻一档 / 轻二档）；
3. **求对侧形**：把模板移到对侧起点（`j0=0` 时对侧为 8，反之亦然）同法定标 → `f_other`；
4. **成分归一**：`parts = {main:1, m1:f_m1, m2:f_m2, other:f_other}`，归一到 Σ = 1；
5. **平均取代率**（按「被标记位点数 / 8」加权）：
   ```
   sites = { main: (1 if j0==8 else 0),  m1: 7/8,  m2: 6/8,  other: (0 if j0==8 else 1) }
   平均取代率 = Σ(parts_k × sites_k) / Σ parts_k × 100%
   ```
   同时给出 **`main_frac` = 全标记分子比例**（= 主形占比）。
6. **检出限提示**：`floor_ratio = floor / A0`（`floor` = 窗内最小正强度），即「最小可测峰 / 主形」，判断结果是否可信。

### 4.3 两把尺子（**必须分开报**）

| 尺子 | 定义 | 说明 |
|---|---|---|
| **平均同位素取代率** | Σ(各物种占比 × 该物种被标记位点数 / 8) | 与「修饰成功比例」经济含义最接近；**质心法对它严格线性**。**默认报这个** |
| **全标记分子比例** | 8 个位点全被标记的分子占比 | 与上一项的差来自「轻一档」物种，差值可达 1 个百分点以上 |

### 4.4 判据与可靠性

- 判读：`≥90%` → 全标记形（标记成功）；`≤5%` → 天然形（未标记）；其余 → 混合。
- **z=2 常不可靠**：窗内 73.6% 强度来自质心噪声峰（20260914 批次），结果仅作参考、**不参与取值**。
- ¹³C 与 ¹⁵N 的位移在 30k 分辨率下**不可分辨**（z=3 差约 0.006 Da）→ 只能说「平均取代率」，**不能分元素**。

**代码坐标**：`同位素取代率计算/embedded_engine.py::centroid_cal`（质心法） / `envelope_peaks` + `neighbor_analysis`（包络序号法） / `auto_calibrate`（质量轴自标定）；批次脚本 `解卷积/20260914分析/analyze_20260914.py`

### 4.5 ⚠️ 自动标定 `auto_calibrate` 的已知退化（2026-09-24 复现，**修复待用户确认**）

- **触发**：锚点**全部来自同一参照形、同一电荷态**（理论 y 值全同）→ 最小二乘退化解 **`a ≈ 1e-16`、`c ≈ 942.5`**，质量轴**塌缩**（所有 x 都算成 942.5）→ 取代率全 0。
- **常见诱因**：窄 RT 窗（如 261–266 s）下只有 z=3 找得到锚点 → 3 个 STD 给出 3 个 y 相同的锚点。
- **验收缺失**：`produce_outputs` 只判 `len(anchors) >= 3` 就采纳，**无 `a > 0` / y 跨度校验**；失败 note 写「沿用输入表 a/c」但**并未回退**。
- **绕过**：输入表 **B15 = 否**（直接用已知 a/c 算），或 RT 窗放宽到覆盖整峰（258–272 s）。两者都能给出 ISO1_004 z=3 **≈99.0%**、STD_005 **≈0.1%**。
- 完整复现表见 `AGENTS.md` 坑 #10。**已交付的 98.9% 不走此函数**（来自 `analyze_20260914.py` 自带的 `calibrate()`）→ 不受影响。

---

## 5. 链路 E：数据读取与质量轴

### 5.1 岛津 QTOF `.lcd`（`lcd_io.py`）

`.lcd` = **OLE 复合文档**，用 `olefile` 直读。

| 项 | 值 |
|---|---|
| 质心数据流 | `QTFL RawData/Centroid Index`（**24 B/记录**，各扫描块偏移表）+ `QTFL RawData/Centroid Data` |
| 扫描头 | **72 B**：`+4` RT（**毫秒**）、`+24` payload 字节数、`+36` 强度宽度 `w ∈ {1,2,4}` |
| payload | `N×u64(质量轴存值 x) + N×w(强度)`，`N = payload / (8 + w)`；x = u64 / 1e12 |

**⚠️⚠️ 质量轴不是线性的（本项目最贵的一个坑）**：
```
m/z = a·x² + c       20260914 批实测 a = 4.53758732e-04,  c = −0.44325,  残差 ±10 ppm
```
- **`openszraw` 文档写的 `mz = x/1e12` 对本变体不适用**（`lcd2csv.py` 正是这么干的，⚠️ 勿用于定量）。
- 线性换算会把真实的 **942.15 显示成 1442**、**944.81 显示成 1444**，且偏差**随 m/z 漂移** → 一度导致「这批数据不含 SPH20291」的**错误结论**（已在多处更正）。
- 只用 `a·x²`（漏掉 `c`）会留 ±90~220 ppm 残差 → **伪装成 ~+5% 的假富集**。
- **标定方法**（`calibrate_tof(anchors)`）：用已知锚点 `[(x_实测, m/z_理论)]` 对 `a·x² + c` 做**最小二乘**。典型用天然形/全标记形的 z=2/3/4 M0 共 6 个锚点。
- `a, c` **依赖仪器/采集模式，换批或换模式必须重标**（文件头 `A_DEFAULT`/`C_DEFAULT` 只是本批默认值）。
- **自检**：标定后 z=3 的 M0 应落在 **天然 942.141 / 全标记 944.813**（±0.01 Da）。

**RT 单位是秒**（用户口述常用分钟，4.4 min = 264 s）。

### 5.2 mzML（`mzml_io.py`）
流式解析、小端 `<d`/`<f>` 解码；自动 RT 窗口；质心谱按 **0.001 Da** 分箱累加为轮廓谱；窗口强度提取（积分 / 峰顶）。

### 5.3 `.jdx`（`jdx2csv.py`）
JCAMP-DX 质心谱直读。**历史教训**：Aug 那批曾以 `.jdx` 为准而误判 `.lcd` 异常 —— 实为 `.lcd` 读取换算错，`.jdx` 本身没问题。

### 5.4 方法文件 `.lcm` / `.lcb`
**没有化合物条目**（全扫描采集）→ **别指望从数据文件里读出目标分子式**，必须由用户提供或从文档获取。

**代码坐标**：`lcd_io.py::read_scans_x/calibrate_tof/set_calibration/detect_rt_window/window_profile/read_lcd`　`mzml_io.py::read_mzml/detect_rt_window`

---

## 6. 链路 F：ChemDraw 结构解析（`chemdraw/`）

`.cdxml`（化学标记 XML）→ 解析原子/键 → 肽序列 / 分子式。

- `chemdraw/parse_cdxml.py`、`peptide_seq.py`、`residue_detail.py`、`cdxml_to_rdkit.py`：结构解析与残基/序列提取。
- **`imp.py` 的回归依据**：`test_imp_docx.py` 对照 ChemDraw 参考文档
  （`参考数据/SPH20291-Isotop1所有可能 (1).docx`，内嵌 **21 个** ChemDraw OLE 结构，分子式经 `olefile` + `CONTENTS` 提取）
  → 输入为全标记物种，`imp.py` 应枚举出**其余 20 种**（¹³C k=0..6 × ¹⁵N j=0..2 全组合，去掉输入本身）。**结论：完全一致。**
- 另注：`gen_isotope_data.py` + `isotope_data.json` 是**从 molmass 提取 19 种元素的全部稳定同位素**，供 Excel 的 `IsotopeData` 工作表作**只读数据源**（不参与计算）——**与那 20 个杂质无关**，别混淆。

---

## 7. 代码坐标速查

| 需要什么 | 去哪 |
|---|---|
| 理论同位素谱 | `theo.py::compute_theoretical_spectrum` |
| 杂质枚举 | `imp.py::enumerate_impurities` |
| 9 档模型 / 5 ppm 重叠系数 | `同位素杂质计算/model.py::build_model` `window_overlap` `m0_coeff` `base_coeff` |
| 9 档扣除引擎 | `同位素杂质计算/calc.py::analyze_profile`（`analyze_sample` 是它的文件薄封装） |
| `.lcd` 读取 + TOF 标定 | `同位素杂质计算/lcd_io.py` |
| mzML 读取 | `同位素杂质计算/mzml_io.py` |
| **原生报告格式生成器** | `同位素杂质计算/run_all.py::write_sp003_workbook` / `write_std_workbook` / `validate_std` |
| `.lcd` 批次一体入口 | `同位素杂质计算/run_lcd_impurity.py` |
| 取代率引擎（自包含） | `同位素取代率计算/embedded_engine.py` |
| NNLS 解卷积（小分子） | `deconv.py`；外壳 `deconv_excel.py` |
| `.lcd` → CSV（⚠️换算错，勿用） | `lcd2csv.py` |
| `.jdx` → CSV | `jdx2csv.py` |
| ChemDraw 解析 | `chemdraw/parse_cdxml.py` |
| imp 枚举的回归测试 | `test_imp_docx.py`（对照 ChemDraw 参考文档的 21 物种） |

---

## 8. 复现命令

```bash
cd "D:/code test/chem/同位素计算及解卷积"
PY="C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe"

"$PY" 同位素杂质计算/model.py                 # 理论层：20 杂质 / 9 档，z=3 tier0 M0=944.8125
"$PY" chem.py theo SPH20291-Isotope1 --top-n 15   # 理论谱
"$PY" chem.py imp  SPH20291-Isotope1              # 枚举 20 个杂质
cd "同位素杂质计算" && "$PY" run_lcd_impurity.py  # C 线：.lcd 批次 → 原生格式 Excel（数据目录在脚本顶部指定）
cd "../同位素取代率计算" && "$PY" _selftest.py     # D 线：离线自测（自动造输入表 → 跑引擎 → 打印结果）
```
> ⚠️ `_selftest.py` 的输入表是 **auto_cal=是 + 窄窗 261–266**，恰好命中 §4.5 的退化 bug（`a≈1e-16`）。要看正常结果，把该脚本生成的输入表 B15 改成「否」（或把 B11/D11 改成 258/272）再跑。

**⚠️ 产出线 B 没有 Excel 前端**：`同位素取代率计算/` 下既无 `.xlsm` 也无 `build_xlsm.py`。引擎 `embedded_engine.py` 用 `--xlsm FILE.xlsm` 读「输入」表跑（单元格布局见其文件头），或直接跑 `_selftest.py`。**不要去找一个不存在的 xlsm**（历史文档曾误引此路径）。

**回归测试**：改计算链路后跑一次**合成谱闭环**（用理论包络合成一条已知答案的谱，喂回引擎看能否还原）——历史脚本见 AGENTS.md §8 第 3 条。

---

## 9. 算法层面的常见误用（与 `AGENTS.md` 坑清单互补）

| 误用 | 后果 | 正确做法 |
|---|---|---|
| 用 openszraw 线性换算读 `.lcd` | 假峰位（942→1442），化合物归属全错 | `m/z = a·x² + c` + 锚点最小二乘标定 |
| 漏掉常数 `c` | 90–220 ppm 残差，伪装成 +5% 富集 | 保留 `c`，或重标定 |
| 用「排序序号」表示质量偏移 | ¹³C/¹⁵N 双峰使序号错位 | 用「锚点窗内全部峰概率之和」 |
| 用 NNLS 求 134-C 分子的逐杂质含量 | 解非唯一，还原和 > 200% | 9 档固定系数 + 逐级三角扣除 |
| 把理论谱的相对丰度当相对含量 | 归一化基准不同（max=100% vs 总和=100%） | 分开书写、分别归一 |
| 用窄 RT 窗取峰 | 宽峰被截断，tier1 从 3.90% 漂到 5.01% | 用 `frac=0.1` 自动窗覆盖整峰 |
| 把「积分偏高」当普适规律 | 偏向随数据抖动方向反转 | 四法并列，主值取 M0·积分 |
| 认为 M0 法与基峰法严格等价 | 链端无对偶档，扣除不闭合 | 视为「量级一致」，基峰法仅作校验 |
| 从 `.lcm`/`.lcb` 里找目标分子式 | 方法文件无化合物条目（全扫描） | 向用户索取或从文档获取 |
| 用质心法结果直接回答「杂质含量」 | 它是平均取代率，非档分布 | 口径不同，见 §0 与 §4.3 |
